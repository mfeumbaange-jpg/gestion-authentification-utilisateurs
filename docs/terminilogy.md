1️⃣ Dépôt (Repository)

Définition :
Un dépôt est l’espace de stockage du projet sur GitHub. Il contient tous les fichiers, dossiers et l’historique des modifications.

Exemple (projet) :
Le dépôt football-analysis contient les analyses de la Premier League, de la La Liga et de la Serie A.

2️⃣ Commit

Définition :
Un commit est une sauvegarde d’un ensemble de modifications effectuées sur le projet, accompagnée d’un message explicatif.

Exemple (projet) :
Commit : "Initialiser la page de connexion pour la Premier League" après création de src/premier_league_login.php.

3️⃣ Branche (Branch)

Définition :
Une branche est une version parallèle du projet permettant de travailler sans affecter la version principale.

Exemple (projet) :
La branche feature/premier-league-analysis est utilisée pour travailler uniquement sur l’analyse de la Premier League.

4️⃣ Merge

Définition :
Un merge est l’action de fusionner les modifications d’une branche dans une autre (souvent vers la branche principale).

Exemple (projet) :
Fusion de la branche feature/liga-analysis dans main après validation du travail sur la Liga.

5️⃣ Pull Request (PR)

Définition :
Une Pull Request est une demande de fusion d’une branche vers une autre, avec revue par les autres membres de l’équipe.

Exemple (projet) :
Ouverture d’une PR pour intégrer la fonctionnalité de connexion de la Premier League dans la branche main.

6️⃣ README.md

Définition :
Le fichier README.md est la page de présentation du projet. Il explique l’objectif du projet et comment l’utiliser.

Exemple (projet) :
Le README.md explique comment lancer l’analyse des données des trois championnats.

7️⃣ .gitignore

Définition :
Le fichier .gitignore indique à Git quels fichiers ne doivent pas être enregistrés dans le dépôt.

Exemple (projet) :
Le fichier .env contenant des mots de passe est ignoré grâce au .gitignore.

8️⃣ Visibilité du dépôt (Public / Privé)

Définition :
La visibilité indique si le dépôt est accessible à tout le monde (public) ou seulement aux membres autorisés (privé).

Exemple (projet) :
Le dépôt est privé pour protéger les données sensibles du projet d’analyse de football.

9️⃣ PAT (Personal Access Token)

Définition :
Un PAT est un jeton d’accès sécurisé utilisé à la place du mot de passe pour accéder à un dépôt via le terminal.

Exemple (projet) :
Un PAT est utilisé pour cloner le dépôt privé football-analysis depuis un ordinateur personnel.

🔟 Protection de branche

Définition :
La protection de branche empêche les modifications directes sur une branche importante sans validation.

Exemple (projet) :
La branche main est protégée : on ne peut fusionner qu’après une Pull Request approuvée par un collègue.
